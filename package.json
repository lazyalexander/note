{
  "name": "note",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal TUI-style static blog for GitHub Pages",
  "scripts": {
    "build": "node src/build.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "marked": "^15.0.0"
  },
  "type": "module"
}
