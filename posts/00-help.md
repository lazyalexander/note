# 命令说明

@help @meta

在首页终端输入命令，回车执行。

## /goto

按标题跳转文章。输入 `/goto` 加空格后再打关键字，会列出匹配标题；↑↓ 或 Tab 选择，回车打开。

```
/goto 欢迎
/goto wel
```

## /tag

按标签筛选文章。标签写在标题下第一行，以 `@` 开头、空格分隔：

```
# 标题

@intro @meta
```

然后：

```
/tag intro
/tag meta
```

## /help

打开本页。
